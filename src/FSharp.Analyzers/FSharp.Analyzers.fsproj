<?xml version="1.0" encoding="utf-8"?>
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- Matches FSharp.Analyzers.SDK's own lib and the fsharp-analyzers CLI tool, both of which
         ship as net8.0. Targeting anything newer stops those hosts from loading the analyzers. -->
    <TargetFramework>net8.0</TargetFramework>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <IsPackable>true</IsPackable>
    <AssemblyName>Idura.FSharp.Analyzers</AssemblyName>
    <PackageId>Idura.FSharp.Analyzers</PackageId>
    <AssemblyVersion>1.4.0.0</AssemblyVersion>
    <Version>1.4.0</Version>
    <Company>Idura</Company>
    <Authors>Frederik Krogsdal Jacobsen</Authors>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <Description>
      A set of F# analyzers which warn against code issues previously encountered by Idura.
    </Description>
    <RepositoryUrl>https://github.com/criipto/fsharp-analyzers</RepositoryUrl>
    <Copyright>Copyright Idura © $([System.DateTime]::UtcNow.Year)</Copyright>
    <PackageTags>F#, fsharp, analyzers, fsharp-analyzer</PackageTags>
    <DevelopmentDependency>true</DevelopmentDependency>
    <SuppressDependenciesWhenPacking>true</SuppressDependenciesWhenPacking>
    <TargetsForTfmSpecificContentInPackage>$(TargetsForTfmSpecificContentInPackage);_AddAnalyzersToOutput</TargetsForTfmSpecificContentInPackage>
    <IncludeBuildOutput>false</IncludeBuildOutput>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Update="FSharp.Core" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="FSharp.Analyzers.SDK" />
  </ItemGroup>
  <ItemGroup>
    <None Include="$(SolutionDir)/README.md" Visible="false" Pack="true" PackagePath="" />
  </ItemGroup>
  <ItemGroup>
    <Compile Include="TypeNames.fs" />
    <Compile Include="MissingUnitArgumentFactAnalyzer.fs" />
    <Compile Include="MissingTypeAnnotationInlineDataAnalyzer.fs" />
    <Compile Include="DoNotUseYourOwnRandomAnalyzer.fs" />
    <Compile Include="DoubleWrappedResultAnalyzer.fs" />
    <Compile Include="LetWildcardResultAnalyzer.fs" />
    <Compile Include="DoResultIgnoreAnalyzer.fs" />
  </ItemGroup>
  <Target Name="_AddAnalyzersToOutput">
    <ItemGroup>
      <TfmSpecificPackageFile Include="$(OutputPath)\$(AssemblyName).dll" PackagePath="analyzers/dotnet/fs" />
    </ItemGroup>
  </Target>
</Project>
